# Copy this file to `.env` and fill in your own key:  cp .env.example .env
# Get a key at https://data.nasdaq.com/account/profile
# (free signup; the Sharadar SF1 fundamentals table needs a paid subscription).
# `.env` is gitignored — only this template (without a real key) is committed.
NASDAQ_DATA_LINK_API_KEY=
